let IndexController = function () {    let ajaxRequest = function (option) {        $.getJSON(Routing.generate('notify_ajax'), {"id": option.id, "state": option.state}).done(function (result) {            $.notify({                // options                icon: result.icon,                title: result.success,                message: result.message,            }, {                // settings                element: 'body',                position: null,                type: result.type,                allow_dismiss: true,                newest_on_top: false,                showProgressbar: false,                placement: {                    from: "top",                    align: "right"                },                offset: 20,                spacing: 10,                z_index: 1031,                delay: 2000,                timer: 500,                url_target: '_blank',                mouse_over: null,                animate: {                    enter: 'animated fadeInDown',                    exit: 'animated fadeOutUp'                },                onShow: null,                onShown: null,                onClose: null,                onClosed: null,                icon_type: 'class',                template: '<div data-notify="container" class="col-xs-11 col-sm-3 alert alert-{0}" role="alert">' +                '<button type="button" aria-hidden="true" class="close" data-notify="dismiss">×</button>' +                '<span data-notify="icon"></span> ' +                '<b><span data-notify="title">{1}</span></b> ' +                '<span data-notify="message">{2}</span>' +                '<div class="progress" data-notify="progressbar">' +                '<div class="progress-bar progress-bar-{0}" role="progressbar" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100" style="width: 0%;"></div>' +                '</div>' +                '</div>'            });        });    }    let toogleInput = function () {        // let that = this;        $('input[data-toggle="toggle"]').change(function () {            var state = $(this).prop('checked');            var id = $.trim($(this).attr('data-notify-id'));            var name = $.trim($(this).attr('data-notify-name'));            var data = $(this).attr('data-size');            if (id) {                ajaxRequest({id: id, state: state})            }        });        return false;    }    let collapse = function () {        $('.collapse').on('shown.bs.collapse', function () {            $(this).parent().find('#btn-'+$(this).attr('id')).html('<i class="fa fa-minus-circle"></i>');        }).on('hidden.bs.collapse', function () {            $(this).parent().find('#btn-'+$(this).attr('id')).html('<i class="fa fa-plus-circle"></i>');            // $(this).parent().find('#btn-'+$(this).attr('id')).html('<i class="fa fa-plus-circle"></i> Afficher');        });    }    return {        init: function () {            toogleInput();            collapse();        }    }}();jQuery(document).ready(function () {    IndexController.init();});